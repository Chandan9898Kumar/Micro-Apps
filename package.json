{
	"private": true,
	"workspaces": {
		"packages": [
			"app1",
			"app2",
			"container"
		]
	},
	"scripts": {
		"start": "lerna run --parallel start",
		"build": "lerna run build",
		"serve": "lerna run --parallel serve",
		"clean": "lerna run --parallel clean",
		"prepare": "lerna run yarn",
		"kill-ports": "kill-ports.bat",
		"fresh-start": "npm run kill-ports && npm start"
	},
	"devDependencies": {
		"lerna": "3.22.1"
	}
}
